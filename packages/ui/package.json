{
  "name": "@journeylab/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JourneyLab design system — STEP-003.01 delivers tokens only; components follow in .02-.04",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "tokens:build": "node --experimental-strip-types tools/gen-tokens.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "7.0.2",
    "vitest": "^3.2.0"
  }
}
